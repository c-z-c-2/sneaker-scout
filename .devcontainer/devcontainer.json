{
  "name": "Sneaker Scout Dev",
  "build": {
    "context": "..",
    "dockerfile": "Dockerfile"
  },
  "workspaceFolder": "/workspace",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached",
  "mounts": [
    "source=${localWorkspaceFolder}/.claude,target=/home/chris/.claude,type=bind,consistency=cached"
  ],
  "remoteUser": "chris",
  "containerUser": "chris",
  "postCreateCommand": "git config --global --add safe.directory /workspace && git config --global --add safe.directory /workspace/aussie-kicks-tracker && git config --global --add safe.directory /workspace/sneaker-scout-backend && chmod 700 /workspace/.beads 2>/dev/null; curl -fsSL https://claude.ai/install.sh | bash && curl -fsSL https://raw.githubusercontent.com/gastownhall/beads/main/scripts/install.sh | bash && curl -fsSL https://raw.githubusercontent.com/Dicklesworthstone/beads_viewer/main/install.sh | bash && echo 'export PATH=\"$HOME/.local/bin:$PATH\"' >> /home/chris/.bashrc && export PATH=\"$HOME/.local/bin:$PATH\" && (claude plugin marketplace add anthropics/claude-plugins-official 2>/dev/null || true) && (claude plugin install superpowers@claude-plugins-official 2>/dev/null || true) && cd /workspace/sneaker-scout-backend && uv sync && echo 'source /workspace/sneaker-scout-backend/.venv/bin/activate' >> /home/chris/.bashrc && echo 'source /workspace/sneaker-scout-backend/.venv/bin/activate' >> /home/chris/.zshrc && cd /workspace/aussie-kicks-tracker && npm ci",
  "postStartCommand": "bd dolt start 2>/dev/null || true",
  "forwardPorts": [8080],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "dbaeumer.vscode-eslint",
        "bradlc.vscode-tailwindcss"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "zsh"
      }
    }
  },
  "remoteEnv": {
    "CHROME_BIN": "/usr/bin/chromium",
    "CHROMEDRIVER_PATH": "/usr/bin/chromedriver",
    "PYTHONPATH": "/workspace",
    "PATH": "${containerEnv:PATH}:/home/chris/.local/bin",
    "BEADS_DOLT_DATA_DIR": "/tmp/beads-dolt"
  }
}
